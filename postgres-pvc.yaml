apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  finalizers:
  - kubernetes.io/pvc-protection
  name: db-pvc
  namepsace: mediacms
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
